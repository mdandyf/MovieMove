
const initialState = {
    
}

const mainReducer = (state = initialState, action={}) => {
    switch (action) {
        case "":
            
        default:
            return state;
    }
}

export default mainReducer;